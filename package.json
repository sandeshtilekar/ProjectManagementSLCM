{
  "name": "ensono-datagrid",
  "version": "1.0.1",
  "description": "Ensono DataGrid — Built by Sandesh Tilekar",
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "start": "bash railway-start.sh",
    "install:all": "cd server && npm install && cd ../client && npm ci"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
